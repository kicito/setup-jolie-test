on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: Setup Jolie
    steps:
      - uses: actions/checkout@v3
      - id: setup-jolie
        name: Setup Jolie
        uses: kicito/setup-jolie@v1
      - id: run_jolie
        name: Run Jolie
        run: jolie main.ol
        shell: bash